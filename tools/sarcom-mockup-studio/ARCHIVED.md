# ARCHIVED

This tool is archived. Use `tools/sarcom-kiosk-lab` instead.

The tldraw/React mockup studio is no longer developed.
The production kiosk stack is Rust + egui + walkers + PMTiles.
The lab tool matches that stack.
